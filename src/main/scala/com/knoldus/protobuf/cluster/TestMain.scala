/*
package com.knoldus.protobuf.cluster

import akka.actor.{Actor, ActorSystem, Props}

class TestActor extends Actor {
    override def receive : Receive = {
        case _ => println(" ?????????????????????????? ")
    }
}

object TestMain extends App
{/*
    val system = ActorSystem("Test")
    val ref = system.actorOf(Props(classOf[TestActor]))


    val proto = GameMessageProto(
        msg = "Pong",
        status = Some(true),
        stage = Some(StageProto(Some(LevelProto(1)))),
        currentLevel = 1,
        optionCurrentLevel = Some(LevelProto(1)),

    )
    println(" >>>>>>>>>>>>>>>>>>>>>>>> " + proto.toByteArray)*/
}
*/
